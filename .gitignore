# server case database — user credentials and client company cases: NEVER commit
data/
data-backups/

# proprietary source material (M. El-Ashry Excel toolset + training slides)
oil excel/
gas excel/
training slids/
*.pptx
*.docx
*.xls
*.xlsx
*.xlsm

node_modules/
*.log
.DS_Store
backup/

# portable build outputs — the exe is ~94 MB and is rebuilt by build.ps1
/WellSim.exe
/build/
