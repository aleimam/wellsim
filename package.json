{
  "name": "wellsim",
  "version": "0.1.0",
  "description": "Oil & gas well nodal analysis, reserve estimation and production forecast — JS port of the M. El-Ashry Excel toolset",
  "type": "module",
  "private": true,
  "scripts": {
    "test": "node --test",
    "test:tenancy": "node --test tests/tenancy.postgres.test.js",
    "start": "node src/server/server.js",
    "build:portable": "powershell -ExecutionPolicy Bypass -File build.ps1"
  },
  "devDependencies": {
    "@electric-sql/pglite": "^0.5.8",
    "esbuild": "^0.28.2",
    "postject": "^1.0.0-alpha.6"
  },
  "dependencies": {
    "pg": "^8.23.0"
  }
}
